{
  "name": "gpt5story",
  "private": true,
  "version": "0.1.0",
  "packageManager": "npm@10.8.1",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.build.json",
    "clean": "rimraf dist && rimraf packages/*/dist",
    "test": "vitest run",
    "longform:run": "tsx scripts/run-longform.ts",
    "longform:run:live": "tsx scripts/run-longform.ts --live"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "rimraf": "^5.0.5",
    "tsx": "^4.19.1",
    "typescript": "^5.4.5",
    "vitest": "^4.0.3"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "jsonrepair": "^3.13.1",
    "zod": "^3.23.8"
  }
}
